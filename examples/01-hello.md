# Hello.

A grayscale document, printed with discipline.

Body copy is IBM Plex Sans at 11pt. Mono is JetBrains — reserved for the things
that should read as system output: the heading above, `inline code`, and the
page number at the foot of the page.

No color. No shadows. No rounded corners. The whole page is the same quiet
object on every printer.
