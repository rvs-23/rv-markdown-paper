# Hello.

A grayscale document, printed with discipline.

Body copy is Archivo at 10.5pt. Mono is JetBrains — reserved for the things
that should read as system output: `inline code`, and the italic folio at the
foot of the page set in Instrument Serif.

No color. No shadows. No rounded corners. The whole page is the same quiet
object on every printer.
